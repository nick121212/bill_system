{
  "name": "@bill/database",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "type": "commonjs",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rimraf dist && tsc -p tsconfig.json && tsc -p tsconfig.esm.json ",
    "start:dev": "tsc -p tsconfig.json && tscpaths -p tsconfig.json -s ./src -o ./dist"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "class-transformer": "^0.5.1",
    "class-validator": "^0.14.1",
    "lodash": "^4.17.21",
    "rimraf": "^6.0.1",
    "ts-loader": "^9.5.2",
    "typescript": "^5.8.2"
  },
  "devDependencies": {
    "tscpaths": "^0.0.9",
    "webpack": "^5.98.0",
    "webpack-cli": "^6.0.1"
  }
}
