{
  "name": "bill_system",
  "private": false,
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {
    "@bill/database": "^1.0.0"
  },
  "devDependencies": {
    "lerna": "^8.2.0"
  },
  "scripts": {
    "start:dev": "lerna run start:dev",
    "doc": "npx @compodoc/compodoc -p packages/server/tsconfig.json -s"
  }
}
